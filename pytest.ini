[pytest]
markers =
    smoke: core happy-path tests — run first, fast
    regression: full boundary value and field validation coverage
    negative: error handling, invalid inputs, edge cases

addopts =
    -v
    --html=reports/test-report.html
    --self-contained-html

testpaths = tests
